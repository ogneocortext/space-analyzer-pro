<Page
    x:Class="SpaceAnalyzer.Views.ScanPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:SpaceAnalyzer.ViewModels"
    xmlns:models="using:SpaceAnalyzer.Services"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:ScanViewModel x:Name="VM"/>
    </Page.DataContext>

    <Grid Padding="24">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="16" MaxWidth="1000" HorizontalAlignment="Center">

                <!-- Header -->
                <StackPanel Spacing="4">
                    <TextBlock Text="New Scan" FontSize="24" FontWeight="Bold"/>
                    <TextBlock Text="Select a directory to analyze disk usage." FontSize="14" Opacity="0.6"/>
                </StackPanel>

                <!-- Path Selection -->
                <Border Style="{StaticResource DashboardCardBorder}">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Directory" FontSize="14" FontWeight="SemiBold"/>
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{x:Bind VM.ScanPath, Mode=TwoWay}" PlaceholderText="Select a directory to scan..."/>
                            <Button Grid.Column="1" Content="Browse" Click="Browse_Click"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Options -->
                <Border Style="{StaticResource DashboardCardBorder}">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Scan Options" FontSize="14" FontWeight="SemiBold"/>
                        <CheckBox Content="Deep scan (unlimited depth)" IsChecked="{x:Bind VM.DeepScan, Mode=TwoWay}"/>
                        <CheckBox Content="Include hidden files and folders" IsChecked="{x:Bind VM.IncludeHidden, Mode=TwoWay}"/>
                    </StackPanel>
                </Border>

                <!-- Scan Button -->
                <Button Content="Start Scan" Click="Scan_Click"
                        HorizontalAlignment="Stretch" Padding="16"
                        IsEnabled="{x:Bind VM.IsNotScanning, Mode=OneWay}">
                    <Button.Resources>
                        <SolidColorBrush x:Key="ButtonBackground" Color="{ThemeResource AccentTextFillColorPrimaryBrush.Color}"/>
                    </Button.Resources>
                </Button>

                <!-- Status -->
                <TextBlock Text="{x:Bind VM.StatusMessage, Mode=OneWay}" FontSize="14" Opacity="0.7"/>

                <!-- Results -->
                <Border Style="{StaticResource DashboardCardBorder}" Visibility="{x:Bind VM.HasResultVisibility, Mode=OneWay}">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Scan Results" FontSize="18" FontWeight="SemiBold"/>

                        <Grid ColumnSpacing="24">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Spacing="4">
                                <TextBlock Text="{x:Bind VM.ResultFilesDisplay, Mode=OneWay}" FontSize="28" FontWeight="Bold"/>
                                <TextBlock Text="Files" FontSize="12" Opacity="0.6"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Spacing="4">
                                <TextBlock Text="{x:Bind VM.ResultSizeDisplay, Mode=OneWay}" FontSize="28" FontWeight="Bold"/>
                                <TextBlock Text="Total Size" FontSize="12" Opacity="0.6"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" Spacing="4">
                                <TextBlock Text="{x:Bind VM.ResultDurationDisplay, Mode=OneWay}" FontSize="28" FontWeight="Bold"/>
                                <TextBlock Text="Duration" FontSize="12" Opacity="0.6"/>
                            </StackPanel>
                            <StackPanel Grid.Column="3" Spacing="4">
                                <TextBlock Text="{x:Bind VM.ResultDirsDisplay, Mode=OneWay}" FontSize="28" FontWeight="Bold"/>
                                <TextBlock Text="Top Dirs" FontSize="12" Opacity="0.6"/>
                            </StackPanel>
                        </Grid>

                        <!-- Top Directories -->
                        <TextBlock Text="Largest Directories" FontSize="14" FontWeight="SemiBold" Margin="0,12,0,0"/>
                        <ItemsRepeater ItemsSource="{x:Bind VM.LastResult.TopDirectories, Mode=OneWay}">
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate x:DataType="models:DirEntry">
                                    <StackPanel Spacing="4" Margin="0,4">
                                        <TextBlock FontSize="13" Text="{x:Bind Name, Mode=OneWay}" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock FontSize="12" Opacity="0.6">
                                            <Run Text="{x:Bind TotalSize, Mode=OneWay}"/>
                                            <Run Text=" bytes"/>
                                            <Run Text=" · "/>
                                            <Run Text="{x:Bind FileCount, Mode=OneWay}"/>
                                            <Run Text=" files"/>
                                        </TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
