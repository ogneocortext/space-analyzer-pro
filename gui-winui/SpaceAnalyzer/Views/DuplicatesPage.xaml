<Page
    x:Class="SpaceAnalyzer.Views.DuplicatesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:SpaceAnalyzer.ViewModels"
        xmlns:models="using:SpaceAnalyzer.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    NavigationCacheMode="Required"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:DuplicatesViewModel x:Name="VM"/>
    </Page.DataContext>

    <Grid Padding="24">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="24" MaxWidth="1000" HorizontalAlignment="Center">

                <!-- Header -->
                <StackPanel Spacing="8">
                    <TextBlock Text="Duplicate Files" FontSize="24" FontWeight="Bold"/>
                    <TextBlock Text="Find and remove duplicate files to reclaim disk space." FontSize="14" Opacity="0.8"/>
                </StackPanel>

                <!-- Path Selection -->
                <Border Style="{StaticResource DashboardCardBorder}" Padding="16">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Directory" FontSize="14" FontWeight="SemiBold"/>
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0" Text="{x:Bind VM.ScanPath, Mode=TwoWay}" PlaceholderText="Select a directory to analyze..." AutomationProperties.Name="Scan directory path"/>
                            <Button Grid.Column="1" Content="Browse" Click="Browse_Click" Padding="12,8" AutomationProperties.Name="Browse"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Analyze Button -->
                <Button Content="Analyze Duplicates" Click="Analyze_Click"
                        HorizontalAlignment="Stretch" Padding="16"
                        IsEnabled="{x:Bind VM.IsNotScanning, Mode=OneWay}"/>

                <!-- Status -->
                <TextBlock Text="{x:Bind VM.StatusMessage, Mode=OneWay}" FontSize="14" Opacity="0.8"/>

                <!-- Results -->
                <Border Style="{StaticResource DashboardCardBorder}" Padding="16" Visibility="{x:Bind VM.HasResultVisibility, Mode=OneWay}">
                    <StackPanel Spacing="16">
                        <TextBlock Text="Analysis Results" FontSize="18" FontWeight="SemiBold"/>

                        <!-- Summary Stats -->
                        <Grid ColumnSpacing="24">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" Spacing="6">
                                <TextBlock Text="{x:Bind VM.DuplicateGroupCount, Mode=OneWay}" FontSize="28" FontWeight="Bold"/>
                                <TextBlock Text="Groups" FontSize="12" Opacity="0.8"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Spacing="6">
                                <TextBlock Text="{x:Bind VM.TotalDuplicateFiles, Mode=OneWay}" FontSize="28" FontWeight="Bold"/>
                                <TextBlock Text="Files" FontSize="12" Opacity="0.8"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" Spacing="6">
                                <TextBlock Text="{x:Bind VM.PotentialSavingsDisplay, Mode=OneWay}" FontSize="28" FontWeight="Bold"/>
                                <TextBlock Text="Reclaimable" FontSize="12" Opacity="0.8"/>
                            </StackPanel>
                        </Grid>

                        <!-- Duplicate Groups List -->
                        <TextBlock Text="Duplicate Groups" FontSize="14" FontWeight="SemiBold" Margin="0,16,0,0"/>
                        <ItemsRepeater ItemsSource="{x:Bind VM.DuplicateGroups, Mode=OneWay}">
                            <ItemsRepeater.Layout>
                                <StackLayout Orientation="Vertical" Spacing="12"/>
                            </ItemsRepeater.Layout>
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate x:DataType="models:DuplicateGroup">
                                    <Border Padding="16" CornerRadius="6" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                                        <StackPanel Spacing="8">
                                            <StackPanel Orientation="Horizontal" Spacing="12">
                                                <TextBlock FontSize="14" FontWeight="SemiBold" Text="{x:Bind Hash, Mode=OneTime}" TextTrimming="CharacterEllipsis"/>
                                                <TextBlock FontSize="13" Opacity="0.8" Text="{x:Bind SizeDisplay, Mode=OneTime}"/>
                                            </StackPanel>
                                            <TextBlock FontSize="13" Opacity="0.8">
                                                <Run Text="{x:Bind FileCount, Mode=OneTime}"/>
                                                <Run Text=" copies"/>
                                                <Run Text=" · "/>
                                                <Run Text="{x:Bind WastedDisplay, Mode=OneTime}"/>
                                                <Run Text=" wasted"/>
                                            </TextBlock>
                                            <ItemsRepeater ItemsSource="{x:Bind Files, Mode=OneTime}">
                                                <ItemsRepeater.ItemTemplate>
                                                    <DataTemplate x:DataType="x:String">
                                                        <TextBlock FontSize="13" Opacity="0.7" Text="{x:Bind}" TextTrimming="CharacterEllipsis" Margin="0,4"/>
                                                    </DataTemplate>
                                                </ItemsRepeater.ItemTemplate>
                                            </ItemsRepeater>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
